import React from 'react'
import { Progress } from 'semantic-ui-react'

const ProgressBar = (props) => <Progress percent={props.percent}  error > time until next service</Progress> 

export default ProgressBar